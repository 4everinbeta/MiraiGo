---
phase: 01-intent-capture-clarification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/schemas/search.py
  - src/app/api/v1/search.py
  - src/app/services/clarification.py
  - src/tests/services/test_clarification_loop.py
  - web/src/lib/api.ts
  - web/src/components/search/__tests__/ClarificationFlow.test.tsx
autonomous: true
requirements: [INTENT-02, INTENT-03, INTENT-04]
must_haves:
  truths:
    - "After submitting a travel prompt, the user receives one clear follow-up question when important details are still missing (per D-01, D-03, D-04)."
    - "When the user answers a follow-up, previously provided constraints remain intact and only the addressed detail changes (per D-05, D-06, D-07)."
    - "The user can see a clarification recap state that explicitly distinguishes known values from 'I don't know' responses before recommendations continue (per D-08, D-14, D-15)."
  artifacts:
    - path: "src/app/services/clarification.py"
      provides: "Slot metadata, missing-slot detection, follow-up question selection, related-slot graph"
      min_lines: 80
    - path: "src/app/schemas/search.py"
      provides: "Clarification request/response types including explicit unknown-slot representation"
      contains: "class ClarificationState"
    - path: "web/src/lib/api.ts"
      provides: "Frontend types for clarification payload and answer/edit request fields"
      contains: "interface ClarificationState"
  key_links:
    - from: "src/app/api/v1/search.py"
      to: "src/app/schemas/search.py"
      via: "POST /search request/response models"
      pattern: "response_model=SearchResponse"
    - from: "web/src/lib/api.ts"
      to: "src/app/schemas/search.py"
      via: "matching contract fields for clarification_state"
      pattern: "clarification_state"
---

<objective>
Define the interface contracts and test scaffolding that all clarification logic and UI will build on.

Purpose: Prevent scavenger-hunt implementation by locking slot definitions, follow-up shape, and merge/update payloads before behavior work starts.
Output: Typed backend/frontend clarification contracts, dependency graph constants, and Wave 0 test files for clarification flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intent-capture-clarification/01-CONTEXT.md
@.planning/phases/01-intent-capture-clarification/01-RESEARCH.md
@.planning/phases/01-intent-capture-clarification/01-UI-SPEC.md
@src/app/schemas/search.py
@src/app/api/v1/search.py
@web/src/lib/api.ts

<interfaces>
From src/app/schemas/search.py:
```python
class SearchRequest(BaseModel):
    query: str | None
    destination: str | None
    origin: str | None
    date_range: SearchDateRange | None
```

From src/app/api/v1/search.py:
```python
@router.post("/search", response_model=SearchResponse)
async def search(request: SearchRequest, db: Session = Depends(get_db)) -> SearchResponse:
    return await search_service.search(request, db=db)
```

From web/src/lib/api.ts:
```typescript
export interface SearchRequest { query?: string; destination?: string; date_range?: SearchDateRange }
export interface SearchResponse { warnings: string[]; results: SearchResult[] }
export async function searchTrips(payload: SearchRequest): Promise<SearchResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical clarification contracts and dependency graph constants</name>
  <read_first>
    - src/app/schemas/search.py
    - src/app/api/v1/search.py
    - .planning/phases/01-intent-capture-clarification/01-CONTEXT.md
    - .planning/phases/01-intent-capture-clarification/01-RESEARCH.md
  </read_first>
  <files>src/app/schemas/search.py, src/app/services/clarification.py, src/app/api/v1/search.py</files>
  <action>Add first-class clarification models in `search.py` and wire them to `SearchRequest`/`SearchResponse`: slot states for destination/timeline/trip_length/budget, confidence score, ambiguity flag, explicit unknown flag, next_question payload, recap payload, and answer/edit request fields. Implement `src/app/services/clarification.py` with constants and pure helpers: critical slot list/order (per D-02, D-04), global confidence threshold constant + docstring rationale (per D-10), one-question selection logic (per D-03), stop condition when all slots are known/unknown (per D-14), and explicit related-slot dependency graph for chip edits (D-16). Keep route signature typed to new request/response contract without adding new endpoint paths.</action>
  <verify>
    <automated>PYTHONPATH=. pytest src/tests/schemas/test_search_schemas.py -k "clarification or search_request" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class ClarificationState|class ClarificationQuestion|class ClarificationRecap" src/app/schemas/search.py` returns matches.
    - `rg -n "CRITICAL_SLOT_ORDER|GLOBAL_CONFIDENCE_THRESHOLD|RELATED_SLOT_GRAPH" src/app/services/clarification.py` returns matches.
    - `rg -n "clarification_state" src/app/schemas/search.py src/app/api/v1/search.py` returns matches in both files.
  </acceptance_criteria>
  <done>Backend exposes explicit clarification contract and helper module that encode D-02/D-03/D-04/D-10/D-14/D-16 with no placeholder logic.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror clarification contracts in frontend API client types</name>
  <read_first>
    - web/src/lib/api.ts
    - src/app/schemas/search.py
    - .planning/phases/01-intent-capture-clarification/01-UI-SPEC.md
  </read_first>
  <files>web/src/lib/api.ts</files>
  <action>Extend `SearchRequest`/`SearchResponse` TypeScript interfaces so frontend can send follow-up answers and recap edits and receive typed `clarification_state` + recap chip data. Preserve existing `searchTrips` transport path and include explicit unknown-state representation (D-08), merge-safe update payload fields (D-05/D-06), and iterative continuation semantics (D-07) in the request contract; do not introduce `any`.</action>
  <verify>
    <automated>cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "interface ClarificationState|interface ClarificationQuestion|explicit_unknown" web/src/lib/api.ts` returns matches.
    - `rg -n "clarification_answer|recap_edit|constraint_updates" web/src/lib/api.ts` returns matches.
    - `rg -n "export interface SearchResponse" web/src/lib/api.ts` block includes `clarification_state`.
  </acceptance_criteria>
  <done>Frontend API layer can express every clarification turn and recap edit without ad-hoc shape casting.</done>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 clarification test scaffolds that enforce the new contracts</name>
  <read_first>
    - src/tests/api/test_search.py
    - src/tests/schemas/test_search_schemas.py
    - web/src/components/search/__tests__/SearchForm.test.tsx
  </read_first>
  <files>src/tests/services/test_clarification_loop.py, web/src/components/search/__tests__/ClarificationFlow.test.tsx</files>
  <action>Create focused tests that currently validate contract-level behavior: backend tests assert priority ordering and one-question selection APIs from `clarification.py`; frontend test asserts typed rendering prerequisites for active question + recap chip shape (no full UI redesign yet). Ensure tests are real assertions, not skipped placeholders, so downstream plans run RED/GREEN against concrete fixtures.</action>
  <verify>
    <automated>PYTHONPATH=. pytest src/tests/services/test_clarification_loop.py -x && cd web && npm test -- --runInBand --testPathPatterns=ClarificationFlow.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/tests/services/test_clarification_loop.py` exits 0.
    - `test -f web/src/components/search/__tests__/ClarificationFlow.test.tsx` exits 0.
    - `rg -n "CRITICAL_SLOT_ORDER|GLOBAL_CONFIDENCE_THRESHOLD|next_question" src/tests/services/test_clarification_loop.py` returns matches.
  </acceptance_criteria>
  <done>Wave 0 tests exist and execute for contract surfaces needed by backend and frontend implementation plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→POST /api/v1/search | Untrusted NL prompt + follow-up answer payload enters backend |
| backend clarification helpers→schema validation | Parsed/normalized slot state crosses into typed persistence/response shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/app/services/clarification.py` slot update inputs | mitigate | Encode allowed slot keys via Enum/Literal and reject unknown slot names at schema boundary. |
| T-01-02 | D | `SearchRequest.query` and clarification answer text | mitigate | Preserve `max_length` and add max length constraints for answer/edit fields in Pydantic models. |
| T-01-03 | I | `warnings` and recap content returned to UI | mitigate | Keep warning payload user-safe strings; do not expose stack traces or provider internals in contract fields. |
</threat_model>

<verification>
Run:
- `PYTHONPATH=. pytest src/tests/schemas/test_search_schemas.py -k "clarification or search_request" -x`
- `PYTHONPATH=. pytest src/tests/services/test_clarification_loop.py -x`
- `cd web && npm test -- --runInBand --testPathPatterns=ClarificationFlow.test.tsx`
</verification>

<success_criteria>
- Clarification contract types exist in backend and frontend and are grep-verifiable.
- `clarification.py` encodes ordered critical slots + global confidence threshold + related-slot graph with no TODO markers.
- Wave 0 clarification tests execute and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-intent-capture-clarification/01-intent-capture-clarification-01-SUMMARY.md`
</output>
