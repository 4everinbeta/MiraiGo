---
phase: 01-intent-capture-clarification
plan: 04
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - web/src/app/page.tsx
  - web/src/components/search/SearchForm.tsx
  - web/src/__tests__/Home.test.tsx
  - src/tests/services/test_clarification_loop.py
autonomous: true
gap_closure: true
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04]
must_haves:
  truths:
    - "After answering trip length, budget, and weather clarifications, clicking Continue to Recommendations keeps those resolved constraints in the same search turn."
    - "Continue turn does not reopen trip length or budget follow-up prompts when those slots were already resolved or explicitly unknown."
    - "Iterative clarification state remains stable across frontend continue payload and backend request resolution."
  artifacts:
    - path: "web/src/app/page.tsx"
      provides: "Turn-session persistence for all resolved critical/non-critical clarification fields including trip_length_days, budget_range, and weather_preference"
      min_lines: 120
    - path: "web/src/components/search/SearchForm.tsx"
      provides: "Continue action submits preserved turn session constraints instead of base-only request"
      min_lines: 300
    - path: "web/src/__tests__/Home.test.tsx"
      provides: "Regression proving continue payload preserves resolved constraints"
      min_lines: 180
    - path: "src/tests/services/test_clarification_loop.py"
      provides: "Service regression proving preserved constraints do not re-open resolved critical slots"
      min_lines: 140
  key_links:
    - from: "web/src/components/search/SearchForm.tsx"
      to: "web/src/app/page.tsx::resolveTurnRequest"
      via: "onSearch continue payload with preserved constraint fields"
      pattern: "continueToRecommendations|onSearch"
    - from: "web/src/app/page.tsx"
      to: "src/app/services/search.py::_resolve_request"
      via: "searchTrips(turnRequest) with persisted trip_length_days/budget_range/weather_preference"
      pattern: "trip_length_days|budget_range|weather_preference"
---

<objective>
Close the Phase 01 verification gap where continue-turn drops resolved clarification constraints and reopens clarification.

Purpose: Restore same-session continuity required by INTENT-04 and locked merge behavior decisions (D-05, D-06, D-07, D-14) without reopening completed slots.
Output: Updated frontend turn persistence/wiring plus targeted regression tests proving continue-turn retains trip length, budget, and weather constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intent-capture-clarification/01-CONTEXT.md
@.planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
@web/src/components/search/SearchForm.tsx
@web/src/app/page.tsx
@src/app/services/search.py
@web/src/__tests__/Home.test.tsx
@src/tests/services/test_clarification_loop.py

<interfaces>
From web/src/app/page.tsx:
```typescript
interface TurnSessionState {
  query?: string
  inventory: SearchRequest['inventory']
  destination?: string
  origin?: string
  date_range?: SearchRequest['date_range']
  travelers: SearchRequest['travelers']
  stay_filters: SearchRequest['stay_filters']
  flight_filters: SearchRequest['flight_filters']
  currency_code: string
  limit_per_provider: number
}
```

From src/app/services/search.py:
```python
def _resolve_request(self, request: SearchRequest) -> tuple[SearchRequest, list[str], ClarificationState]:
    slot_states = self._build_slot_states(request, intent)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist full resolved clarification constraints in frontend turn session and continue payload</name>
  <read_first>
    - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
    - web/src/app/page.tsx
    - web/src/components/search/SearchForm.tsx
    - web/src/lib/api.ts
  </read_first>
  <files>web/src/app/page.tsx, web/src/components/search/SearchForm.tsx</files>
  <behavior>
    - Test 1: turn session stores resolved `trip_length_days`, `budget_range`, and `weather_preference` from applied filters and carries them into subsequent turn requests (D-05, D-07).
    - Test 2: continue action reuses preserved turn-session data rather than `buildBaseRequest(query)`-only payload, so latest resolved constraints are retained (INTENT-04, D-05).
    - Test 3: latest explicit user answers still override previous extracted values while preserving existing resolved slots not edited in current turn (D-06).
  </behavior>
  <action>Extend `TurnSessionState` and `resolveTurnRequest` in `page.tsx` to include and persist `trip_length_days`, `budget_range`, and `weather_preference` (plus any existing resolved fields already tracked). Update `setTurnSession` to hydrate these from `nextResponse.applied_filters` and from request payload fallbacks so no resolved slot is dropped between turns. In `SearchForm.tsx`, change `continueToRecommendations` to submit a request that preserves session constraints via page-level merge logic instead of base-only request construction; keep CTA text and one-question UX unchanged (D-14/D-15). Do not introduce new clarification logic in UI; this fix is continuity-only for the failed gap.</action>
  <verify>
    <automated>cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "trip_length_days|budget_range|weather_preference" web/src/app/page.tsx` returns matches in `TurnSessionState`, `resolveTurnRequest`, and `setTurnSession`.
    - `rg -n "continueToRecommendations" web/src/components/search/SearchForm.tsx` shows continue payload path that does not drop preserved constraints.
    - `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx` passes.
  </acceptance_criteria>
  <done>Continue-turn request construction preserves all resolved clarification constraints required for same-session continuation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage preventing continue-turn slot loss and clarification reopen</name>
  <read_first>
    - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
    - web/src/__tests__/Home.test.tsx
    - src/tests/services/test_clarification_loop.py
    - src/app/services/search.py
  </read_first>
  <files>web/src/__tests__/Home.test.tsx, src/tests/services/test_clarification_loop.py</files>
  <behavior>
    - Test 1: frontend continue flow submits preserved `trip_length_days`, `budget_range`, and `weather_preference` after clarification answers (INTENT-04).
    - Test 2: backend `_resolve_request` receives preserved fields and keeps `trip_length` and `budget` slots resolved (no follow-up reopen) on continue turn (D-05, D-07).
    - Test 3: weather state remains present across continue turn and does not regress critical-slot completion behavior (INTENT-02, INTENT-04).
  </behavior>
  <action>Add a focused regression in `Home.test.tsx` that reproduces the verification scenario: answer trip length and budget, click Continue to Recommendations, assert outgoing request still contains resolved `trip_length_days`, `budget_range`, and weather preference fields. Add a service-level regression in `test_clarification_loop.py` executing sequential turns and asserting continue turn returns no reopened `trip_length`/`budget` prompt when preserved fields are present. Keep test fixtures minimal and deterministic; no broad test rewrites.</action>
  <verify>
    <automated>(cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx) && PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "continue|trip_length_days|budget_range|weather_preference" web/src/__tests__/Home.test.tsx` returns new regression assertions.
    - `rg -n "continue|trip_length|budget|weather" src/tests/services/test_clarification_loop.py` returns new sequential-turn regression test.
    - `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx` passes.
    - `PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py -x` passes.
  </acceptance_criteria>
  <done>Automated regressions fail before fix and pass after fix, locking the continue-turn continuity behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SearchForm continue action → page turn payload assembly | User-initiated action must not drop already-resolved constraints in request construction |
| page turn payload → backend `_resolve_request` | Payload integrity determines whether critical slots stay resolved or re-open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | `web/src/app/page.tsx` turn merge logic | mitigate | Explicitly merge/persist allowlisted resolved fields (`trip_length_days`, `budget_range`, `weather_preference`) and avoid lossy base-request-only construction. |
| T-01-11 | D | clarification loop continuity | mitigate | Regression tests enforce that continue-turn does not trigger unnecessary extra clarification turns for already-resolved critical slots. |
| T-01-12 | R | turn history/answer continuity | mitigate | Preserve latest explicit answers across continue turns and assert persistence in frontend/backend regression tests for auditability. |
</threat_model>

<verification>
Run:
- `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx`
- `PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py -x`
</verification>

<success_criteria>
- Continue CTA no longer drops resolved trip length, budget, or weather constraints.
- Continue turn proceeds with resolved state instead of reopening trip length/budget clarification.
- Verification gap for INTENT-04 is reproducibly closed by automated regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-intent-capture-clarification/01-intent-capture-clarification-04-SUMMARY.md`
</output>
