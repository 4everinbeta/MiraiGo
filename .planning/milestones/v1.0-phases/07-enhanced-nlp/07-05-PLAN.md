---
phase: 07-enhanced-nlp
plan: 05
type: plan
wave: 4
depends_on: [07-03-PLAN.md]
gap_closure: true
files_expected:
  - web/src/app/page.tsx
  - web/src/components/search/SearchForm.tsx
  - web/src/__tests__/Home.test.tsx
requirements: [INTENT-04]
---

# Phase 07 Plan 05: Clarification slot continuity regression fix

## Objective

Fix the clarification-loop regression where updating one slot (e.g., timeline) drops a previously resolved slot (e.g., trip length), causing repetitive loops.

## Gap Source

- 07-UAT test 2: user-reported slot value loss after sequential clarification edits.

## Tasks

1. Trace and harden turn-session merge logic so resolved slots are preserved unless explicitly edited/cleared.
2. Ensure recap-edit and clarification-answer flows share one deterministic merge policy for `trip_length_days`, timeline/date range, budget, and weather.
3. Add regression coverage proving sequential edits do not drop prior resolved fields.

## Verification

- `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx`

## Success Criteria

- Sequential clarification updates preserve previously resolved values.
- Next-question progression no longer loops because of dropped slot state.
