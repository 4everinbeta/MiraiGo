---
phase: 08-intent-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/api/test_search.py
  - src/tests/services/test_clarification_loop.py
  - web/src/__tests__/Home.test.tsx
  - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
  - .planning/phases/08-intent-verification-closure/08-VALIDATION.md
autonomous: true
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04]
must_haves:
  truths:
    - "INTENT-01..04 all have fresh automated evidence with no skipped INTENT-critical checks (per D8-01, D8-02)."
    - "Automated closure gate hard-fails unless blocked=0 and skipped=0 for INTENT-critical checks (per D8-02)."
  artifacts:
    - path: "src/tests/api/test_search.py"
      provides: "API clarification stability and extraction coverage"
    - path: "src/tests/services/test_clarification_loop.py"
      provides: "clarification sequencing/continuity regression coverage"
    - path: "web/src/__tests__/Home.test.tsx"
      provides: "UI turn continuity coverage"
    - path: ".planning/phases/01-intent-capture-clarification/01-VERIFICATION.md"
      provides: "updated automated evidence references for INTENT-01..04"
  key_links:
    - from: "src/tests/api/test_search.py + src/tests/services/test_clarification_loop.py + web/src/__tests__/Home.test.tsx"
      to: ".planning/phases/08-intent-verification-closure/08-VALIDATION.md"
      via: "strict gate evidence update"
      pattern: "blocked:\\s*0|skipped:\\s*0"
    - from: ".planning/phases/08-intent-verification-closure/08-VALIDATION.md"
      to: ".planning/phases/01-intent-capture-clarification/01-VERIFICATION.md"
      via: "INTENT-01..04 evidence propagation"
      pattern: "INTENT-0[1-4]"
---

<objective>
Lock Phase 8 automated INTENT verification to a strict green baseline with auditable blocked/skipped gate enforcement.

Purpose: satisfy D8-01 and D8-02 for automated proof before human UAT closure.
Output: passing automated INTENT suites plus strict gate evidence in closure artifacts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-intent-verification-closure/08-CONTEXT.md
@.planning/phases/08-intent-verification-closure/08-RESEARCH.md
@.planning/phases/08-intent-verification-closure/08-VALIDATION.md
@.planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
@.planning/phases/07-enhanced-nlp/07-UAT.md
@src/tests/services/test_clarification_loop.py
@src/tests/api/test_search.py
@web/src/__tests__/Home.test.tsx

<interfaces>
From `src/tests/api/test_search.py`:
```python
def test_search_handles_follow_up_clarification_turn(...)
```

From `src/tests/services/test_clarification_loop.py`:
```python
def test_continue_turn_with_preserved_resolved_fields(...)
```

From `web/src/__tests__/Home.test.tsx`:
```ts
it("preserves resolved clarification fields on Continue")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce full automated INTENT evidence matrix (per D8-01)</name>
  <read_first>
    - .planning/phases/08-intent-verification-closure/08-CONTEXT.md
    - .planning/phases/07-enhanced-nlp/07-UAT.md
    - src/tests/api/test_search.py
    - src/tests/services/test_clarification_loop.py
    - web/src/__tests__/Home.test.tsx
  </read_first>
  <files>src/tests/api/test_search.py, src/tests/services/test_clarification_loop.py, web/src/__tests__/Home.test.tsx</files>
  <behavior>
    - Test 1: INTENT-01 free-form prompt start flow stays green in UI/API tests.
    - Test 2: INTENT-02 structured extraction remains stable across multilingual/ambiguous prompts.
    - Test 3: INTENT-03 focused one-question clarification ordering remains deterministic.
    - Test 4: INTENT-04 turn continuity keeps resolved slots through continue path.
  </behavior>
  <action>Backfill/adjust tests so INTENT-01..04 each have explicit automated assertions and no INTENT-critical skip path remains (specifically replace skipped API clarification stability coverage noted in Phase 07 UAT). Keep this plan scoped to test/evidence updates only; defer runtime remediation code edits to 08-02 per D8-03.</action>
  <verify>
    <automated>PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py src/tests/api/test_search.py -q && cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false</automated>
  </verify>
  <acceptance_criteria>
    - Each INTENT requirement maps to at least one passing automated assertion.
    - INTENT-critical skip count is zero.
  </acceptance_criteria>
  <done>Automated INTENT matrix is explicit, reproducible, and fully green.</done>
</task>

<task type="auto">
  <name>Task 2: Record strict automated gate results with hard-fail criteria (per D8-02)</name>
  <read_first>
    - .planning/phases/08-intent-verification-closure/08-VALIDATION.md
    - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
    - Task 1 command output
  </read_first>
  <files>.planning/phases/08-intent-verification-closure/08-VALIDATION.md</files>
  <action>Update `08-VALIDATION.md` with a strict INTENT gate snapshot that includes explicit `blocked:` and `skipped:` totals for INTENT-critical checks and requirement mapping rows for INTENT-01..04. Gate policy is hard-fail unless both values are zero (per D8-02).</action>
  <verify>
    <automated>PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py src/tests/api/test_search.py -q && cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false && cd .. && grep -Eq "blocked:\\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md && grep -Eq "skipped:\\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-VALIDATION.md` includes INTENT-01..04 gate rows with explicit counts.
    - Verification command exits non-zero whenever blocked or skipped is not zero.
  </acceptance_criteria>
  <done>Automated strict gate evidence is present and machine-checkable.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh automated evidence artifacts for closure traceability</name>
  <read_first>
    - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
    - .planning/phases/08-intent-verification-closure/08-VALIDATION.md
  </read_first>
  <files>.planning/phases/01-intent-capture-clarification/01-VERIFICATION.md, .planning/phases/08-intent-verification-closure/08-VALIDATION.md</files>
  <action>Update `01-VERIFICATION.md` automated evidence sections with latest command outputs, explicit INTENT-01..04 mapping, and strict-gate statements: `blocked: 0` and `skipped: 0` for INTENT-critical checks (per D8-02). Keep remediation ownership in 08-02/08-03 per D8-03.</action>
  <verify>
    <automated>grep -Eq "INTENT-01|INTENT-02|INTENT-03|INTENT-04" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md && grep -Eq "blocked:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md && grep -Eq "skipped:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Both artifacts contain fresh automated evidence for INTENT-01..04.
    - Gate language explicitly states `blocked: 0` and `skipped: 0`.
  </acceptance_criteria>
  <done>Automated closure evidence is auditable and requirement-traceable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner→closure-artifacts | Evidence from command output is translated into requirement status |
| web client→api search route | Untrusted clarification payloads flow into slot-resolution logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `.planning/phases/01-VERIFICATION.md` | mitigate | Require exact command reruns and update artifact only from fresh outputs; reject stale evidence references. |
| T-08-02 | R | INTENT closure claims | mitigate | Keep command-level evidence + requirement mapping lines in verification artifacts for auditability. |
| T-08-03 | D | clarification submit path (`SearchForm`/`page.tsx`) | mitigate | Preserve `isSubmitting` guards and regression tests asserting no repeated-submit turn duplication. |
</threat_model>

<verification>
Run strict INTENT suite:
- `PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py src/tests/api/test_search.py -q`
- `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false`
- `grep -Eq "blocked:\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md`
- `grep -Eq "skipped:\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md`
</verification>

<success_criteria>
- INTENT-01..04 each have fresh passing automated evidence.
- No skipped INTENT-critical automated checks.
- No open automated blockers remain for INTENT scope.
</success_criteria>

<output>
After completion, create `.planning/phases/08-intent-verification-closure/08-intent-verification-closure-01-SUMMARY.md`
</output>
