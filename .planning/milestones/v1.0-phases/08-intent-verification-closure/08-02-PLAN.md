---
phase: 08-intent-verification-closure
plan: 02
type: execute
wave: 2
depends_on: [08-01-PLAN.md]
files_modified:
  - .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md
  - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
  - .planning/phases/08-intent-verification-closure/08-VALIDATION.md
  - src/app/services/search.py
  - src/app/services/clarification.py
  - web/src/app/page.tsx
  - web/src/components/search/SearchForm.tsx
  - src/tests/services/test_clarification_loop.py
  - src/tests/api/test_search.py
  - web/src/__tests__/Home.test.tsx
autonomous: false
requirements: [INTENT-01, INTENT-02, INTENT-03, INTENT-04]
must_haves:
  truths:
    - "Each INTENT requirement has fresh human UAT pass evidence in addition to automated proof (per D8-01)."
    - "Closure run contains zero blockers and zero skipped INTENT-critical checks (per D8-02)."
    - "Any newly discovered INTENT-critical UAT regression is fixed and retested within this phase (per D8-03)."
  artifacts:
    - path: ".planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md"
      provides: "fresh reproducible human pass/fail evidence for INTENT-01..04"
    - path: ".planning/phases/01-intent-capture-clarification/01-VERIFICATION.md"
      provides: "updated human verification and blocker status"
  key_links:
    - from: "01-HUMAN-UAT.md"
      to: "01-VERIFICATION.md"
      via: "human evidence summary + status transition"
      pattern: "status: complete|human_verification"
    - from: "UAT failures"
      to: "source/test fixes"
      via: "same-phase remediation loop"
      pattern: "fix -> automated rerun -> human rerun"
---

<objective>
Capture fresh human UAT evidence for INTENT-01..04 and enforce strict closure gates with an in-phase remediation loop for any critical failures.

Purpose: satisfy D8-01 through D8-03 on human evidence and closure strictness.
Output: completed human UAT artifact and verification report with zero open blockers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-intent-verification-closure/08-CONTEXT.md
@.planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md
@.planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
@.planning/phases/07-enhanced-nlp/07-UAT.md
@web/src/app/page.tsx
@web/src/components/search/SearchForm.tsx
@src/app/services/search.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare deterministic INTENT human UAT script and evidence template (per D8-01)</name>
  <read_first>
    - .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md
    - .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md
    - .planning/phases/08-intent-verification-closure/08-CONTEXT.md
    - .planning/phases/07-enhanced-nlp/07-UAT.md
  </read_first>
  <files>.planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md</files>
  <action>Replace pending human checks with step-by-step reproducible UAT cases mapped to INTENT-01..04, each with input prompt, follow-up answers, expected UI/API outcomes, and explicit pass/fail fields. Include a mandatory “no skipped INTENT-critical checks” gate line (per D8-02).</action>
  <verify>
    <automated>grep -Eq "INTENT-01|INTENT-02|INTENT-03|INTENT-04" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "blocked:\\s*0|blocked:\\s*\\[pending\\]" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "skipped:\\s*0|skipped:\\s*\\[pending\\]" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT file has four requirement-mapped tests.
    - Each test has deterministic steps and expected outcomes.
  </acceptance_criteria>
  <done>Human UAT template is ready for immediate execution without interpretation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute fresh browser UAT for INTENT closure (per D8-01, D8-02)</name>
  <read_first>
    - .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md
    - web/src/app/page.tsx
    - web/src/components/search/SearchForm.tsx
  </read_first>
  <files>.planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md, .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md</files>
  <action>Execute the documented UAT script exactly, capture pass/fail evidence per INTENT requirement, and block closure until all INTENT-critical checks are executed with no skips (per D8-02). Record explicit `blocked:` and `skipped:` counters in both artifacts.</action>
  <what-built>Prepared reproducible UAT flow with requirement mapping and strict closure gates.</what-built>
  <how-to-verify>
    1. Run backend/frontend locally.
    2. Execute each INTENT-01..04 test exactly as documented in `01-HUMAN-UAT.md`.
    3. Mark pass/fail for each test, including concrete failure notes if any.
    4. Confirm zero skipped INTENT-critical checks and zero blockers before approval.
  </how-to-verify>
  <verify>
    <automated>PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py src/tests/api/test_search.py -q && cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false && cd .. && grep -Eq "blocked:\\s*0" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "skipped:\\s*0" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "blocked:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md && grep -Eq "skipped:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All four INTENT tests are marked pass.
    - skipped=0 and blocked=0.
  </acceptance_criteria>
  <done>Checkpoint evidence is recorded and either approved or routed to Task 3 remediation with exact failing test IDs.</done>
  <resume-signal>Type "approved" if all checks pass, or provide exact failing INTENT test IDs and observed behavior.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remediate UAT failures by layer and re-run strict gate to zero (per D8-03)</name>
  <read_first>
    - checkpoint output from Task 2
    - src/app/services/search.py
    - src/app/services/clarification.py
    - web/src/app/page.tsx
    - web/src/components/search/SearchForm.tsx
    - src/tests/services/test_clarification_loop.py
    - src/tests/api/test_search.py
    - web/src/__tests__/Home.test.tsx
  </read_first>
  <files>src/app/services/search.py, src/app/services/clarification.py, web/src/app/page.tsx, web/src/components/search/SearchForm.tsx, src/tests/services/test_clarification_loop.py, src/tests/api/test_search.py, web/src/__tests__/Home.test.tsx, .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md, .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md, .planning/phases/08-intent-verification-closure/08-VALIDATION.md</files>
  <behavior>
    - Classify each failure as backend-slot/ordering or frontend-turn-continuity before editing.
    - Backend class: touch `search.py`/`clarification.py` plus pytest files only.
    - Frontend class: touch `page.tsx`/`SearchForm.tsx` plus `Home.test.tsx` only.
    - Re-run automated suite, then re-run failed human UAT case(s) to green.
  </behavior>
  <action>If Task 2 reports failures, fix them inside Phase 8 by layer (backend or frontend) with targeted regression-first edits only, then repeat automated + human verification until all INTENT checks pass with `blocked: 0` and `skipped: 0` (per D8-02, D8-03). Always update `01-HUMAN-UAT.md`, `01-VERIFICATION.md`, and `08-VALIDATION.md` counters after reruns so gate state is auditable. If Task 2 is approved with no failures, record a no-fix-needed pass with the same zeroed counters.</action>
  <verify>
    <automated>PYTHONPATH=. ./venv/bin/pytest src/tests/services/test_clarification_loop.py src/tests/api/test_search.py -q && cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false && cd .. && grep -Eq "blocked:\\s*0" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "skipped:\\s*0" .planning/phases/01-intent-capture-clarification/01-HUMAN-UAT.md && grep -Eq "blocked:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md && grep -Eq "skipped:\\s*0" .planning/phases/01-intent-capture-clarification/01-VERIFICATION.md && grep -Eq "blocked:\\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md && grep -Eq "skipped:\\s*0" .planning/phases/08-intent-verification-closure/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All UAT-discovered INTENT failures are closed in-phase.
    - Failure handling is layer-targeted instead of broad cross-stack edits.
    - 01-HUMAN-UAT.md, 01-VERIFICATION.md, and 08-VALIDATION.md all show `blocked: 0` and `skipped: 0`.
  </acceptance_criteria>
  <done>Human verification is fresh, complete, and either remediated or approved with no open INTENT blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester→closure decision | Manual observations determine requirement closure status |
| remediation code path→intent flow | Bug fixes can accidentally regress slot sequencing/continuity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | S | Human UAT attestation | mitigate | Require explicit per-test pass/fail entries with requirement IDs and observed outcomes in `01-HUMAN-UAT.md`. |
| T-08-05 | T | INTENT continuity during remediation | mitigate | Add/adjust regression tests before code fixes and run full INTENT suite after each fix. |
| T-08-06 | R | Closure status transition | mitigate | Update `01-VERIFICATION.md` only after checkpoint evidence shows pass with blocked=0/skipped=0. |
</threat_model>

<verification>
- Execute checkpoint and collect explicit per-requirement outcomes.
- Re-run strict automated INTENT suite after any remediation.
- Hard-fail if any INTENT-critical artifact reports `blocked != 0` or `skipped != 0`.
</verification>

<success_criteria>
- Fresh human UAT evidence exists for INTENT-01..04.
- No skipped INTENT-critical checks.
- No open blockers; any discovered failures are fixed and retested in-phase.
</success_criteria>

<output>
After completion, create `.planning/phases/08-intent-verification-closure/08-intent-verification-closure-02-SUMMARY.md`
</output>
