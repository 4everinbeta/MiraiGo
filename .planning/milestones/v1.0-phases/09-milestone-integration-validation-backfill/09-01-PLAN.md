---
phase: 09-milestone-integration-validation-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/api.ts
  - src/tests/api/test_search.py
  - web/src/__tests__/Home.test.tsx
autonomous: true
requirements:
  - INTENT-02
  - INTENT-04
must_haves:
  truths:
    - "Frontend clarification slot contract includes weather and matches backend canonical enum."
    - "API/UI regression tests explicitly cover weather slot parity paths."
  artifacts:
    - path: web/src/lib/api.ts
      provides: ClarificationSlot parity with backend schema
    - path: src/tests/api/test_search.py
      provides: API regression evidence for weather slot
    - path: web/src/__tests__/Home.test.tsx
      provides: UI continuity evidence including weather slot fixtures
  key_links:
    - from: src/app/schemas/search.py
      to: web/src/lib/api.ts
      via: ClarificationSlot parity
      pattern: weather
    - from: web/src/lib/api.ts
      to: web/src/__tests__/Home.test.tsx
      via: typed clarification fixtures
      pattern: clarification_state
---

<objective>
Fix the open integration contract gap by aligning frontend clarification-slot typing with backend canonical schema (`weather`) and proving parity through API/UI regressions.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Align frontend clarification slot typing with backend canonical enum</name>
  <read_first>
    - src/app/schemas/search.py
    - web/src/lib/api.ts
  </read_first>
  <files>web/src/lib/api.ts</files>
  <action>Update `ClarificationSlot` and related clarification-state typing in `web/src/lib/api.ts` so `weather` is present and no backend slot is omitted.</action>
  <verify>
    <automated>grep -Eq "ClarificationSlot.*weather|'weather'" web/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - Frontend slot union includes `weather`.
    - Existing slot names remain intact.
  </acceptance_criteria>
  <done>Frontend clarification slot contracts are fully aligned with backend canonical schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill regression assertions for weather parity in API and UI tests</name>
  <read_first>
    - src/tests/api/test_search.py
    - web/src/__tests__/Home.test.tsx
    - web/src/lib/api.ts
  </read_first>
  <files>src/tests/api/test_search.py, web/src/__tests__/Home.test.tsx</files>
  <action>Add or strengthen test assertions so weather slot presence is validated in API clarification payloads and UI typed response fixtures.</action>
  <verify>
    <automated>PYTHONPATH=. ./venv/bin/pytest src/tests/api/test_search.py -q && cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false</automated>
  </verify>
  <acceptance_criteria>
    - API suite passes with weather-slot assertions.
    - Home UI suite passes with weather-carrying fixtures.
  </acceptance_criteria>
  <done>Weather slot parity is enforced by passing backend and frontend regression suites.</done>
</task>
</tasks>

<verification>
- `PYTHONPATH=. ./venv/bin/pytest src/tests/api/test_search.py -q`
- `cd web && npm test -- --runInBand --testPathPatterns=Home.test.tsx --watch=false`
- `grep -Eq "weather" web/src/lib/api.ts`
</verification>

<success_criteria>
- Integration gap `INTEG-INTENT-WEATHER-SLOT` has direct code + test evidence.
</success_criteria>
