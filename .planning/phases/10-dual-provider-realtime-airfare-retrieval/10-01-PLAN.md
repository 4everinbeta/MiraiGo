---
phase: 10-dual-provider-realtime-airfare-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/providers/amadeus.py
  - src/app/core/config.py
  - src/tests/providers/test_amadeus.py
autonomous: true
requirements:
  - AIR-01
user_setup:
  - service: amadeus
    why: "Realtime flight offers require OAuth client credentials"
    env_vars:
      - name: AMADEUS_CLIENT_ID
        source: "Amadeus developer dashboard -> API keys"
      - name: AMADEUS_CLIENT_SECRET
        source: "Amadeus developer dashboard -> API keys"
      - name: AMADEUS_API_URL
        source: "Amadeus docs / environment config"
must_haves:
  truths:
    - "Amadeus provider can return mapped flight offers for valid flight requests."
    - "Amadeus auth uses shared cached OAuth token with safety buffer and one forced refresh on first 401."
  artifacts:
    - path: src/app/providers/amadeus.py
      provides: "AmadeusFlightsProvider implementing TravelProvider for InventoryType.FLIGHT"
    - path: src/app/core/config.py
      provides: "Amadeus and deadline/token cache config keys"
    - path: src/tests/providers/test_amadeus.py
      provides: "Provider mapping/auth lifecycle regression coverage"
  key_links:
    - from: src/app/providers/amadeus.py
      to: src/app/db/redis.py
      via: "Redis token get/set with expiry safety buffer"
      pattern: "redis_client\\.(get|setex)"
    - from: src/app/providers/amadeus.py
      to: src/app/providers/base.py
      via: "TravelProvider subclass + ProviderError handling"
      pattern: "class AmadeusFlightsProvider\\(TravelProvider\\)"
---

<objective>
Implement the Amadeus flight provider adapter with Redis-backed OAuth lifecycle and executable provider tests.

Purpose: Satisfy AIR-01 with production-safe auth behavior before orchestration wiring.
Output: New provider module, config surface, and unit tests for mapping and 401 refresh behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-CONTEXT.md
@.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-RESEARCH.md
@src/app/providers/base.py
@src/app/providers/duffel.py
@src/app/schemas/search.py
@src/app/core/config.py
@src/app/db/redis.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Amadeus provider adapter with Redis OAuth token lifecycle</name>
  <read_first>
    - src/app/providers/base.py
    - src/app/providers/duffel.py
    - src/app/schemas/search.py
    - src/app/db/redis.py
    - .planning/phases/10-dual-provider-realtime-airfare-retrieval/10-CONTEXT.md
  </read_first>
  <files>src/app/providers/amadeus.py, src/app/core/config.py, src/tests/providers/test_amadeus.py</files>
  <behavior>
    - Test 1: valid Amadeus offer payload maps to FlightSearchResult with provider="amadeus" and provider_label="Amadeus".
    - Test 2: first 401 triggers one token refresh and one retry, then succeeds or raises ProviderError without infinite retry loop.
  </behavior>
  <action>
    Create `AmadeusFlightsProvider` per D10-09 and D10-11: client_credentials token retrieval, Redis-shared token cache with safety buffer, and single forced refresh on first 401. Map flight-offer response into existing `FlightSearchResult` contract only (no Phase 11 normalization). Add config keys for AMADEUS_* and token safety/deadline settings in `config.py`. In the same task, create initial `src/tests/providers/test_amadeus.py` coverage for `test_amadeus_provider_builds_flight_results` so the task-level verify command is runnable.
  </action>
  <verify>
    <automated>./venv/bin/pytest src/tests/providers/test_amadeus.py::test_amadeus_provider_builds_flight_results -q</automated>
  </verify>
  <acceptance_criteria>Provider returns typed FlightSearchResult entries and never logs/seals raw auth token values.</acceptance_criteria>
  <done>Amadeus provider compiles, uses Redis token cache with safety buffer, maps offers into existing contract fields, and includes runnable baseline provider tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Amadeus provider auth/offer mapping tests</name>
  <read_first>
    - src/tests/api/test_search.py
    - src/tests/test_caching.py
    - src/tests/conftest.py
  </read_first>
  <files>src/tests/providers/test_amadeus.py</files>
  <behavior>
    - Test 1: provider maps minimal valid offer response into FlightSearchResult.
    - Test 2: provider refreshes cached token once on 401 and retries exactly once.
    - Test 3: missing credentials returns configured=false health signal path.
  </behavior>
  <action>
    Create `src/tests/providers/test_amadeus.py` with fake HTTP responses and fake redis interactions to enforce D10-09 token semantics and D10-11 shared cache usage. Keep tests provider-focused; orchestration behavior is covered in Plan 10-02.
  </action>
  <verify>
    <automated>./venv/bin/pytest src/tests/providers/test_amadeus.py::test_amadeus_refreshes_token_once_on_401 -q</automated>
  </verify>
  <acceptance_criteria>Both mapping and 401 refresh tests are green and deterministic under fake redis/http fixtures.</acceptance_criteria>
  <done>Wave-0 provider test gaps for Amadeus are closed with executable test coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→amadeus api | Outbound authenticated calls cross untrusted network |
| app→redis | Token cache persists auth material across instances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I | src/app/providers/amadeus.py | mitigate | Never log Authorization headers/token payloads; keep error text sanitized |
| T-10-02 | D | src/app/providers/amadeus.py | mitigate | Single forced refresh-on-401 only (per D10-09), bounded retries, no retry storm |
| T-10-03 | T | src/app/core/config.py | mitigate | Keep AMADEUS_API_URL config-controlled only; no user-input URL overrides |
</threat_model>

<verification>
- `./venv/bin/pytest src/tests/providers/test_amadeus.py -q`
</verification>

<success_criteria>
- AIR-01 provider adapter exists and returns mapped flight results.
- OAuth token cache is Redis-backed with safety-buffer expiry and one forced refresh on first 401.
- Provider unit tests pass for mapping + refresh behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-01-SUMMARY.md`
</output>
