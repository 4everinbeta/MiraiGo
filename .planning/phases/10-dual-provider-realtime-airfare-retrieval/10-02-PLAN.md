---
phase: 10-dual-provider-realtime-airfare-retrieval
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/providers/registry.py
  - src/app/services/search.py
  - src/tests/services/test_search_dual_provider.py
autonomous: true
requirements:
  - AIR-01
  - AIR-02
must_haves:
  truths:
    - "Search flow can execute both Amadeus and Duffel flight providers with bounded deadlines."
    - "If one provider fails/times out, available provider results still return with explicit warning/status."
    - "Merged flight ordering is deterministic and provider-tagged without client-side resort dependency."
  artifacts:
    - path: src/app/providers/registry.py
      provides: "Dual-provider registry wiring for flight retrieval"
    - path: src/app/services/search.py
      provides: "Hybrid gate/prefetch orchestration, deadlines, partial-success merge behavior"
    - path: src/tests/services/test_search_dual_provider.py
      provides: "Deterministic interleave + partial-failure + deadline coverage"
  key_links:
    - from: src/app/providers/registry.py
      to: src/app/providers/amadeus.py
      via: "provider registry inclusion"
      pattern: "AmadeusFlightsProvider"
    - from: src/app/services/search.py
      to: ProviderExecution
      via: "per-provider timeout + error capture"
      pattern: "asyncio\\.wait_for|error_message"
    - from: src/app/services/search.py
      to: SearchResponse.warnings/provider_status/results
      via: "partial success signaling"
      pattern: "warnings=.*provider_status=.*results="
---

<objective>
Wire dual-provider realtime flight orchestration with deterministic merge and resilient deadline behavior.

Purpose: Deliver end-to-end AIR-01/AIR-02 retrieval behavior in the existing search flow, honoring locked Phase 10 execution rules.
Output: Dual-provider registry, updated SearchService orchestration logic, and service-level tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-CONTEXT.md
@.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-RESEARCH.md
@src/app/providers/registry.py
@src/app/services/search.py
@src/app/schemas/search.py

<interfaces>
From src/app/providers/base.py:
```python
class TravelProvider(ABC):
    provider_name: str
    display_name: str
    inventory_types: tuple[InventoryType, ...]
    @property
    def is_configured(self) -> bool: ...
    async def search(self, request: SearchRequest, inventory_type: InventoryType) -> list[SearchResult]: ...
```

From src/app/services/search.py:
```python
@dataclass
class ProviderExecution:
    provider: TravelProvider
    inventory_type: InventoryType
    configured: bool
    cache_hit: bool
    duration_ms: int
    result_count: int
    results: list[SearchResult]
    error_message: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register Amadeus and enforce hybrid flight trigger policy in SearchService</name>
  <read_first>
    - src/app/providers/registry.py
    - src/app/services/search.py
    - .planning/phases/10-dual-provider-realtime-airfare-retrieval/10-CONTEXT.md
  </read_first>
  <files>src/app/providers/registry.py, src/app/services/search.py, src/tests/services/test_search_dual_provider.py</files>
  <behavior>
    - Test 1: strict visible gate blocks user-visible flight retrieval until origin+destination+date_range and stable clarification state are present.
    - Test 2: bounded prefetch path can run only when destination+timeline are known and must not bypass visible gate.
  </behavior>
  <action>
    Update registry to include `AmadeusFlightsProvider()` plus existing `DuffelFlightsProvider()` for `InventoryType.FLIGHT`. In SearchService implement D10-01/D10-02/D10-03 hybrid policy: bounded prefetch eligibility requires destination+timeline; visible realtime flight results require strict gate (origin+destination+date range + stable clarification turn). Keep Phase 10 scope tight: no normalization fields or contract expansion (deferred to Phase 11). In the same task, add initial `src/tests/services/test_search_dual_provider.py` gate coverage for `test_visible_flight_gate_requires_origin_destination_and_dates` so task-level verify is executable.
  </action>
  <verify>
    <automated>./venv/bin/pytest src/tests/services/test_search_dual_provider.py::test_visible_flight_gate_requires_origin_destination_and_dates -q</automated>
  </verify>
  <acceptance_criteria>Provider fan-out never returns user-visible flight offers when strict gate is unmet, while prefetch eligibility remains separately bounded.</acceptance_criteria>
  <done>Registry and gate logic are updated with runnable baseline dual-provider gate tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-provider deadlines, partial-success signaling, and deterministic interleave merge</name>
  <read_first>
    - src/app/services/search.py
    - src/app/schemas/search.py
    - .planning/phases/10-dual-provider-realtime-airfare-retrieval/10-RESEARCH.md
  </read_first>
  <files>src/app/services/search.py, src/tests/services/test_search_dual_provider.py</files>
  <behavior>
    - Test 1: timeout/failure of one provider still returns results from available provider plus explicit warning.
    - Test 2: merged flight results preserve per-provider internal order and interleave deterministically by score with source tags.
    - Test 3: Duffel path remains valid in dual-provider run.
  </behavior>
  <action>
    Apply D10-04/D10-05/D10-06/D10-07/D10-08/D10-10 in orchestration: per-provider hard deadline (`asyncio.wait_for` or equivalent), best-effort partial success, explicit warning/status when provider fails/unconfigured, and deterministic provider-tagged interleave (preserve provider-local order before interleave by score). Add tests in `test_search_dual_provider.py` for deterministic interleave, partial failure, timeout, and Duffel regression.
  </action>
  <verify>
    <automated>./venv/bin/pytest src/tests/services/test_search_dual_provider.py::test_dual_provider_interleave_is_deterministic src/tests/services/test_search_dual_provider.py::test_partial_failure_returns_other_provider_results src/tests/providers/test_duffel.py -q</automated>
  </verify>
  <acceptance_criteria>Dual-provider orchestration is deadline-bounded, failure-tolerant, explicitly signaled, and stable across repeated runs.</acceptance_criteria>
  <done>AIR-01/AIR-02 orchestration semantics are implemented with green dual-provider service tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service fan-out→provider adapters | Multiple external API calls with variable latency/failure |
| service response→frontend | Warning/status text and ordering affect user trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | D | src/app/services/search.py | mitigate | Per-provider hard timeouts (D10-06) prevent one slow provider from blocking full response |
| T-10-05 | R | src/app/services/search.py | mitigate | Explicit provider warning/status signaling (D10-05) for failed/misconfigured providers to avoid silent degradation |
| T-10-06 | T | src/app/services/search.py | mitigate | Deterministic merge/interleave (D10-08) prevents nondeterministic reorder side effects between runs |
</threat_model>

<verification>
- `./venv/bin/pytest src/tests/services/test_search_dual_provider.py -q`
- `./venv/bin/pytest src/tests/providers/test_duffel.py -q`
</verification>

<success_criteria>
- AIR-01/AIR-02 execute through dual-provider fan-out in one search flow.
- Deadlines and partial success behavior are enforced and observable.
- Merged results remain deterministic and provider-attributed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-02-SUMMARY.md`
</output>
