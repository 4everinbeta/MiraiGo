---
phase: 10-dual-provider-realtime-airfare-retrieval
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - src/tests/api/test_search.py
  - web/src/components/search/ResultsDashboard.tsx
  - web/src/components/search/__tests__/ResultsDashboard.test.tsx
autonomous: true
requirements:
  - AIR-01
  - AIR-02
must_haves:
  truths:
    - "API regression tests prove dual-provider warnings/status and result continuity in the same discovery flow."
    - "UI renders API-provided flight order and explicit degraded messaging accessibly."
  artifacts:
    - path: src/tests/api/test_search.py
      provides: "Search endpoint regression assertions for dual-provider phase behavior"
    - path: web/src/components/search/ResultsDashboard.tsx
      provides: "Accessible warning/status rendering without client-side reorder logic"
    - path: web/src/components/search/__tests__/ResultsDashboard.test.tsx
      provides: "Frontend regression coverage for provider labels, warning visibility, and stable order"
  key_links:
    - from: src/tests/api/test_search.py
      to: /api/v1/search
      via: "integration assertions for provider_status/warnings/results"
      pattern: "client\\.post\\(\"/api/v1/search\""
    - from: web/src/components/search/ResultsDashboard.tsx
      to: response.results
      via: "render in API-provided order"
      pattern: "flightResults\\.map"
---

<objective>
Lock in Phase 10 behavior with API/UI regression coverage and accessibility-safe degraded-state messaging.

Purpose: Prevent regressions in dual-provider retrieval presentation and warning semantics before moving to Phase 11 normalization.
Output: Backend endpoint regression tests and frontend rendering/test updates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-UI-SPEC.md
@src/tests/api/test_search.py
@web/src/components/search/ResultsDashboard.tsx
@web/src/components/search/__tests__/ResultsDashboard.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API dual-provider regression assertions for warnings/status continuity</name>
  <read_first>
    - src/tests/api/test_search.py
    - src/app/api/v1/search.py
    - src/app/services/search.py
    - .planning/phases/10-dual-provider-realtime-airfare-retrieval/10-VALIDATION.md
  </read_first>
  <files>src/tests/api/test_search.py</files>
  <behavior>
    - Test 1: response includes explicit provider_status and warnings when one provider is unavailable (D10-05, D10-10).
    - Test 2: available provider results still return in same `/search` response (D10-04).
  </behavior>
  <action>
    Extend API search tests with dual-provider stubs validating partial success + explicit warning/status in one discovery call, covering AIR-01/AIR-02 flow continuity. Keep contract unchanged (no Phase 11 normalization additions).
  </action>
  <verify>
    <automated>./venv/bin/pytest src/tests/api/test_search.py::test_post_search_partial_dual_provider_failure_keeps_available_results -q</automated>
  </verify>
  <acceptance_criteria>API tests fail if provider failure becomes silent or if available provider results are dropped from response.</acceptance_criteria>
  <done>Endpoint-level regression safety exists for dual-provider failure signaling and continuity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add frontend regression for explicit warnings, aria-live, and API-order rendering</name>
  <read_first>
    - web/src/components/search/ResultsDashboard.tsx
    - web/src/components/search/__tests__/ResultsDashboard.test.tsx
    - .planning/phases/10-dual-provider-realtime-airfare-retrieval/10-UI-SPEC.md
  </read_first>
  <files>web/src/components/search/ResultsDashboard.tsx, web/src/components/search/__tests__/ResultsDashboard.test.tsx</files>
  <behavior>
    - Test 1: degraded flight warnings are rendered in accessible `aria-live="polite"` region.
    - Test 2: provider label remains visible on each flight card.
    - Test 3: flight results display in API-provided order (no client resort).
  </behavior>
  <action>
    Update `ResultsDashboard` and tests to enforce UI-SPEC degraded-state/accessibility/ordering requirements while preserving existing contracts. Explicitly keep Phase 10 scope: consume existing `warnings[]`, `provider_status[]`, and `results[].provider_label` only.
  </action>
  <verify>
    <automated>cd web && npm test -- --runInBand --runTestsByPath src/components/search/__tests__/ResultsDashboard.test.tsx</automated>
  </verify>
  <acceptance_criteria>Frontend test suite fails if warning accessibility or deterministic API-order rendering regresses.</acceptance_criteria>
  <done>UI behavior is regression-locked for degraded messaging, provider attribution, and stable order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response→UI rendering | User-facing trust signals depend on warning/status rendering fidelity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | R | src/tests/api/test_search.py | mitigate | Endpoint regression asserts explicit warning/status, preventing silent degraded behavior |
| T-10-08 | I | web/src/components/search/ResultsDashboard.tsx | mitigate | Accessible warning region (`aria-live="polite"`) ensures degraded-state visibility beyond color cues |
| T-10-09 | T | web/src/components/search/ResultsDashboard.tsx | mitigate | Regression tests enforce API-provided order rendering and prevent accidental client-side resorting |
</threat_model>

<verification>
- `./venv/bin/pytest src/tests/api/test_search.py -q`
- `cd web && npm test -- --runInBand --runTestsByPath src/components/search/__tests__/ResultsDashboard.test.tsx`
</verification>

<success_criteria>
- Dual-provider degraded-state behavior is protected by API and UI regressions.
- Warnings/status are explicit and accessible.
- Phase 10 stays scoped to retrieval/orchestration (no normalization contract expansion).
</success_criteria>

<output>
After completion, create `.planning/phases/10-dual-provider-realtime-airfare-retrieval/10-03-SUMMARY.md`
</output>
